module.exports = {
    name: "help",
    code: `$title[Help Commands]
    $addField[version;View Kirbo's Current Update]
    $addField[settings;manage kirbo's chatbot settings]
    $addField[support;find out who created me and get support!;no]
    $addField[stats;returns stats information about the bot;no]
    $addField[resetchatbot;resets the chatbot channel!;no]
    $addField[setchatbot;set the chatbot channel!;no]
    `
    
    }